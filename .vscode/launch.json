{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}.o",
            "args": [
                "Hello",
                "World"
            ],
            "cwd": "${fileDirname}/",
            "preLaunchTask": "C/C++: g++ build active file"
        }
    ]
}